'use strict';

module.exports = require('../../../../../node_modules/egg');

if (process.execArgv.find(argv => argv.includes('egg-ts-helper'))) {
  setTimeout(() => {
    console.log('exit by master test end');
    process.exit(0);
  }, require('os').platform() === 'win32' ? 11000 : 7000);
}
